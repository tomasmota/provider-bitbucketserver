apiVersion: project.bitbucketserver.crossplane.io/v1alpha1
kind: Project
metadata:
  name: testproject
spec:
  forProvider:
    name: testproject
    key: PRJ
  providerConfigRef:
    name: mybitbucketserver